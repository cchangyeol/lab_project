2026.09.21 토이 프로젝트 하루 일과

프로젝트 주제 - 게임 기록 웹

한 일 :

0. 프로젝트 주제 선정 - 게임 기록 웹

1. 개발 환경 세팅(레포 생성, 깃 연동, MongoDB 연동 및 환경변수 설정)

2. 상태 및 데이터 모양을 정하는 코드 작성
  코드 위치 : src/types/game.ts

  상태 구분 : '하고싶음' | '하는중' | '클리어' | '중단'
  데이터 모양 :
  _id?: string;         // MongoDB가 자동으로 만들어주는 고유 id
  title: string;        // 게임명
  platform: string;     // 플랫폼 (PC, PS5, Switch 등)
  startDate: string;    // 시작일 (YYYY-MM-DD 형태의 문자열)
  endDate?: string;     // 마지막으로 플레이한 날
  playTime: number;     // 플레이 시간 (시간 단위)
  rating: number;       // 평점 (1~5)
  status: GameStatus;   // 상태 (하고싶음/하는중/클리어/중단)
  trailerUrl?: string;  // 트레일러 영상 링크

3. MongoDB에 접속하기 위한 코드 작성
  코드 위치 : src/lib/mongodb.ts
    DB에 연결하는 과정을 좀 더 공부할 필요가 있음

4. 게임 기록을 입력하는 등록 화면 개발
  코드 위치 : src/app/games/new/page.tsx

5. 등록 폼과 연결해서 게임 기록을 저장하는 API 코드 작성
  코드 위치 : src/app/api/games/route.ts
    API 구조를 좀 더 공부할 필요가 있음
    GET과 POST의 개념을 좀 더 상세하게 공부할 필요가 있음

6. 게임 등록 화면 테스트 진행 - MongoDB에 값 정상적으로 들어가는거 확인
